<% if logged_in?(:site_admin) %>
  <nav class="breadcrumb blogcrumb" >
    <%= link_to fa_icon('file-text'), toggle_status_blog_path(f), style: blog_status_color(f) %>
    <%= link_to fa_icon('pencil-square-o'), edit_blog_path(f) %>
    <%= link_to fa_icon('trash'), f, method: :delete, data: { confirm: 'Are you sure?' } %>
  </nav>
<% end %>